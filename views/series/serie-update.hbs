<div class="main-content">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1>Actualizar Serie</h1>
                <hr>
                <form action="/series/{{_id}}/edit" method="POST">
                    <div class="mb-3">
                        <div class="col">
                            <label class="form-label">Título</label>
                            <input type="text" class="form-control" id="emailInput" name="title" value="{{title}}">
                            <div class="form-text">Requerido.</div>
                        </div>
                        <button class="btn btn-dark block my-3" type="submit">Actualizar</button>
                        <a class="btn btn-dark block my-3" href="/series/{{_id}}/delete">Delete</a>


                        {{#if errorMessage}}
                        <div id="errorMessage">
                            <p>{{errorMessage}}</p>
                        </div>
                        {{/if}}
                    </div>
                </form>
            </div>
        </div>

    </div>
</div>